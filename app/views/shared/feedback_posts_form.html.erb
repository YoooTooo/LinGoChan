<%= form_for(@reply_post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field" "フィードバック">
    <%= f.text_area :content, placeholder: "フィードバックを書いてください" %>
  </div>
  <%= f.submit "投稿する！", class: "btn btn-primary" %>
<% end %>
